
import React from 'react';

const LiveTranscriptionFeature: React.FC = () => {
  // This feature has been migrated to an external URL: https://tockyamthanh.abaii.space/
  // The click handler on the homepage now redirects the user.
  // This component is intentionally left empty and returns null.
  // It is preserved to prevent any potential import errors in the app,
  // but it should no longer be rendered by FeatureRenderer.
  return null;
};

export default LiveTranscriptionFeature;
